#include "semcal/backends/lp_backend.h"
#include <sstream>

namespace semcal {
namespace backends {

// LpBackend implementation
// Note: LpBackend is a pure interface, implementations should be in lp_stub.cpp or concrete implementations

} // namespace backends
} // namespace semcal
